# Multi-GPU Programming in CUDA C++ -- Table of Contents (planning document)

Subtitle: From Peer-to-Peer Memory to Distributed Training Across the Cluster

This is the full planned outline. Check this file first before starting any new chapter or appendix. Mirrors the sibling books' own build-verify-lock discipline (see getting-started.md for this book's own specific honesty discipline around no real multi-GPU hardware).

## Part 0 -- Why Multi-GPU, and the Hardware Underneath It
1. Why One GPU Is Not Enough
2. Multi-GPU Hardware Topology: PCIe, NVLink, and NVSwitch
3. The CUDA Multi-GPU Programming Model: Devices, Contexts, and Streams Across Them

## Part 1 -- Getting Data Between GPUs
4. Peer-to-Peer Memory Access and Unified Virtual Addressing
5. Explicit Transfers: cudaMemcpyPeer, Staged Host Transfers, and When Each Wins
6. Streams, Events, and Cross-Device Synchronization
7. CUDA Inter-Process Communication: Sharing Memory and Events Across Processes

## Part 2 -- Collective Communication, Built From Scratch
8. Broadcast and Reduce: The First Two Collectives, By Hand
9. Ring All-Reduce: The Algorithm Behind Every Multi-GPU Training Job
10. All-Gather, Reduce-Scatter, and All-to-All
11. NCCL: What It Actually Does Differently From What You Just Built

## Part 3 -- Parallelization Strategies
12. Data Parallelism: Replicated Model, Sharded Data
13. Model Parallelism: When One GPU Can't Hold the Weights
14. Tensor Parallelism: Splitting a Single Matrix Multiply Across Devices
15. Pipeline Parallelism: Splitting Layers Across Devices, and the Bubble That Costs You
16. Domain Decomposition for Scientific Computing: Halo Exchange and Distributed Stencils

## Part 4 -- Synchronization, Load Balancing, and Failure
17. Barriers and Global Synchronization Across Devices
18. Load Balancing Across Heterogeneous GPUs
19. Stragglers, Failures, and Fault-Tolerant Collectives

## Part 5 -- Scaling Beyond One Node
20. MPI From Scratch, Then CUDA-Aware MPI: Scaling Beyond One Node
21. GPUDirect RDMA: Bypassing the Host Entirely
22. NVSHMEM and GPU-Initiated Communication
23. CUDA Graphs Across Multiple GPUs and Multiple Nodes

## Part 6 -- Case Studies
24. Multi-GPU Dense Matrix Multiplication at Scale
25. Distributed Training of a Neural Network: Data Parallelism and Ring All-Reduce
26. Multi-GPU LLM Inference: Tensor and Pipeline Parallelism in Practice
27. Multi-GPU N-Body Simulation
28. Multi-GPU Graph Processing: Distributed Breadth-First Search
29. A Distributed Jacobi Solver: Multi-GPU Scientific Computing With Halo Exchange
30. Multi-GPU Ray Tracing and Rendering
31. Multi-GPU Monte Carlo Risk Simulation

## Part 7 -- Cutting-Edge Industrial Case Studies
32. Mixture-of-Experts at Scale: Expert Parallelism and GPU-Initiated All-to-All (DeepEP)
33. Disaggregated LLM Inference: Splitting Prefill and Decode Across GPU Pools
34. GPU-Accelerated Recommendation Systems: Hybrid Data- and Model-Parallel Embedding Tables at Scale
35. AI Weather Forecasting at Scale: Multi-GPU Neural Weather Models
36. Multi-GPU XVA and Portfolio Risk Aggregation at Bank Scale
37. Low-Latency Market Data and GPUDirect RDMA in Capital Markets
38. Real-Time Multi-GPU Fraud Detection at Payment Scale
39. Multi-GPU Genomics at Scale: Whole-Genome Secondary Analysis with NVIDIA Clara Parabricks
40. Multi-GPU Medical Image Reconstruction: Distributed Iterative CT Reconstruction at Scale

## Appendices
- A. Installation and Setup -- Multi-GPU Development Without a Multi-GPU Machine
- B. Practice Quiz
- C. NCCL and NVSHMEM: The Standard Libraries You Get for Free
- D. CUDA Graphs and Cooperative Multi-Device Kernels
- E. Profiling and Benchmarking Multi-GPU Communication
- F. From PyTorch Distributed and DeepSpeed to C++: A Rosetta Stone
- G. Common Failure Modes: Deadlocks, Silent Corruption From Missed Synchronization, and Topology Mismatches

## Status
Scaffolded 2026-09-17: mkdocs.yml, docs/index.md, docs/getting-started.md, this file.
Chapter 1 ("Why One GPU Is Not Enough") written 2026-09-17: docs/part0/01-why-one-gpu-is-not-enough.md, with code in docs/part0/code/ (01_device_query.cu, 02_memory_wall.cpp, 03_compute_wall.cpp). All three genuinely compiled and run (nvcc 12.0 via `apt-get install nvidia-cuda-toolkit` -- the pip nvcc route does not actually provide a working nvcc binary, see the corrected getting-started.md).
Chapter 2 ("Multi-GPU Hardware Topology: PCIe, NVLink, and NVSwitch") written 2026-09-17: docs/part0/02-multi-gpu-hardware-topology.md, with code in docs/part0/code/ (04_topology_bandwidth_model.cpp, 05_peer_access_query.cu). Cited real PCIe/NVLink/NVSwitch bandwidth figures (sources at end of chapter file); topology bandwidth model genuinely compiled/run (g++) and deterministic; peer-access query genuinely compiled with real nvcc and run, honestly reporting cudaErrorNoDevice. Chapter 3 ("The CUDA Multi-GPU Programming Model: Devices, Contexts, and Streams Across Them") written 2026-09-17: docs/part0/03-the-cuda-multi-gpu-programming-model.md, with code in docs/part0/code/ (06_single_device_setup.cu, 07_multi_device_loop_pattern.cu). Covers per-thread current-device state, implicit contexts, stream/event device binding (kernel-launch-vs-memcpy enforcement asymmetry), the idiomatic multi-device loop pattern, and the new-thread-defaults-to-device-0 pitfall -- cited to the CUDA Programming Guide's multi-GPU chapter and an NVIDIA dev blog post. Both code files genuinely compiled with real nvcc and run, deterministic. Chapter 4 ("Peer-to-Peer Memory Access and Unified Virtual Addressing") written 2026-09-17: docs/part1/04-peer-to-peer-memory-access-and-uva.md, with code in docs/part1/code/ (08_pointer_attributes.cu, 09_peer_access_setup.cu, 10_p2p_simulation.cpp). This is Part 1's first chapter and the book's first use of the host-side N-buffer simulation technique (see getting-started.md), used in Section 4.3 to verify P2P transfer routing logic (direct write vs. host-staged fallback) against an independent reference, since real peer-to-peer transfer performance cannot be honestly measured without a second real device. Sections 4.1-4.2 cover Unified Virtual Addressing (cudaPointerGetAttributes) and the real, unidirectional peer-access setup sequence (cudaDeviceCanAccessPeer / cudaDeviceEnablePeerAccess), cited to the CUDA Programming Guide's multi-GPU chapter and CUDA Runtime API peer-access docs. Both .cu files genuinely compiled with real nvcc and run, honestly reporting cudaErrorNoDevice; the .cpp simulation genuinely compiled with g++ and run, deterministic PASS/PASS. Written in the DSA-book page format (header box, Intuition/Background split, one COMMON TRAP per section, full code, Chapter Summary/Self-Check Questions/Where We Go Next/Worked Solutions/Sources cited) established during the Chapter 1-3 reformat. Chapter 5 ("Explicit Transfers: cudaMemcpyPeer, Staged Host Transfers, and When Each Wins") written 2026-09-17: docs/part1/05-explicit-transfers-cudamemcpypeer-staged-host-transfers.md, with code in docs/part1/code/ (11_memcpy_peer.cu, 12_staged_host_transfer.cu, 13_when_each_wins_model.cpp). Covers the real cudaMemcpyPeer()/cudaMemcpyPeerAsync() calls and what they fall back to when peer access isn't enabled (5.1), the staged host-fallback made explicit as two cudaMemcpy() calls through a pinned buffer (5.2), and a plain host C++ closed-form cost model reusing Chapter 2's own already-cited PCIe/NVLink bandwidth figures to show peer-direct winning by 2x-3.17x when available, and being the only option at all when it isn't (5.3). Cited to the CUDA Programming Guide's multi-GPU chapter, the CUDA Runtime API reference for cudaMemcpyPeer, and the Asynchronous Execution page for pinned-memory semantics. Both .cu files genuinely compiled with real nvcc and run, honestly reporting cudaErrorNoDevice; the .cpp cost model genuinely compiled with g++ and run, deterministic. Written in the established DSA-book page format from the start. Chapter 6 ("Streams, Events, and Cross-Device Synchronization") written 2026-09-17: docs/part1/06-streams-events-and-cross-device-synchronization.md, with code in docs/part1/code/ (14_event_basics.cu, 15_cross_device_wait.cu, 16_overlap_query.cu). Covers the real event lifecycle -- create/record/query/synchronize (6.1); the real cross-device dependency mechanism, cudaStreamWaitEvent() taking an event from a different device than the waiting stream, contrasted with the needlessly-serializing cudaDeviceSynchronize() (6.2); and what the Runtime API can honestly report about a device's physical capacity for transfer/kernel overlap via asyncEngineCount and concurrentKernels, without running either operation (6.3). Cited to the CUDA Programming Guide's Asynchronous Execution page, a mirrored copy of the CUDA Runtime API's cudaStreamWaitEvent docs (the exact 'may be from a different context ... cross-device synchronization' wording), and the cudaDeviceProp struct reference for asyncEngineCount/concurrentKernels. All three code files genuinely compiled with real nvcc and run, deterministic cudaErrorNoDevice throughout. Written in the established DSA-book page format from the start. Chapter 7 ("CUDA Inter-Process Communication: Sharing Memory and Events Across Processes") written 2026-09-17: docs/part1/07-cuda-inter-process-communication.md, with code in docs/part1/code/ (17_ipc_mem_handle.cu, 18_ipc_event_handle.cu, 19_ipc_handoff_simulation.cpp). Covers the real cudaIpcGetMemHandle()/cudaIpcOpenMemHandle()/cudaIpcCloseMemHandle() sequence for sharing device memory across OS processes, including the documented undefined-behavior warning about freeing before closing (7.1); the real cudaEventCreateWithFlags(DisableTiming|Interprocess)/cudaIpcGetEventHandle()/cudaIpcOpenEventHandle() sequence for sharing events across processes (7.2); and a host-side simulation verifying the one property that actually defines IPC memory sharing -- the importer and exporter share the SAME memory, not independently-updated copies, shown by a live mutation propagating with no transfer call at all (7.3), deliberately NOT simulating the documented UB case since UB has no defined result to check against. Cited to the CUDA Runtime API documentation for cudaIpcGetMemHandle/cudaIpcOpenMemHandle and cudaEventCreateWithFlags. Both .cu files genuinely compiled with real nvcc and run, deterministic cudaErrorNoDevice throughout; the .cpp simulation genuinely compiled with g++ and run, deterministic PASS/PASS. Written in the established DSA-book page format from the start. **This completes Part 1** (Chapters 4-7). Chapter 8 ("Broadcast and Reduce: The First Two Collectives, By Hand") written 2026-09-17: docs/part2/08-broadcast-and-reduce-the-first-two-collectives-by-hand.md, with code in docs/part2/code/ (20_broadcast_by_hand.cu, 21_reduce_by_hand.cu, 22_collectives_simulation.cpp). **Opens Part 2.** Covers a real cudaMemcpyPeer()-loop broadcast from a root device to every non-root device (8.1); a real staged-transfer reduce accumulating every device's buffer on the host (8.2); and a host-side simulation -- now established as Part 2's primary verification tool, since a collective's correctness is inherently a multi-device relationship no single honest API call can confirm -- verifying broadcast produces identical copies at every simulated device and reduce's sum is order-independent for integers, explicitly NOT extended to floating point (cited to NVIDIA's own Floating Point and IEEE 754 documentation on non-associativity) (8.3). Both .cu files genuinely compiled with real nvcc and run, deterministic cudaErrorNoDevice; the .cpp simulation genuinely compiled with g++ and run, deterministic PASS x4. Written in the established DSA-book page format from the start. Chapter 9 ("Ring All-Reduce: The Algorithm Behind Every Multi-GPU Training Job") written 2026-09-18: docs/part2/09-ring-all-reduce-the-algorithm-behind-every-multi-gpu-training-job.md, with code in docs/part2/code/ (23_naive_allreduce.cu, 24_ring_step_by_hand.cu, 25_ring_allreduce_simulation.cpp). Covers the naive reduce-then-broadcast baseline built entirely from Chapter 8's own broadcast and reduce loops, making explicit that every byte passes through the root device twice (9.1); the real, symmetric single-step cudaMemcpyPeer ring exchange with no root device at all (9.2); and a host-side simulation of the full ring all-reduce (scatter-reduce phase then all-gather phase, same communication pattern, add vs. overwrite) verified against an independent reference sum for N=4, plus a real counted data-volume comparison against 9.1's baseline showing the ring's per-device traffic is exactly N/2 cheaper than the naive root's traffic -- consistent with Andrew Gibiansky's cited 2(N-1)K/N closed-form bandwidth-optimal ring formula (9.3). All three code files genuinely compiled (nvcc for the two .cu files, g++ for the .cpp simulation) and run, deterministic. This chapter introduces ASCII diagrams (plain-text box-drawing using only -, |, +, >, ^) at the end of each section's Intuition and as a chapter-opening overview, per explicit user request; Chapter 7 was retrofitted with the same diagrams, and every chapter from here forward uses them. Chapter 10 ("All-Gather, Reduce-Scatter, and All-to-All") written 2026-09-18: docs/part2/10-all-gather-reduce-scatter-and-all-to-all.md, with code in docs/part2/code/ (26_allgather_by_hand.cu, 27_reducescatter_by_hand.cu, 28_alltoall_by_hand.cu, 29_collectives_simulation.cpp). Isolates Chapter 9's own two ring phases and names them: all-gather is Chapter 9's all-gather phase run alone from each device's own original chunk (10.1, direct cudaMemcpyPeer loop, no host, like Chapter 8's broadcast); reduce-scatter is Chapter 9's scatter-reduce phase run alone, stopped before the all-gather that would turn it into a full all-reduce (10.2, host-mediated cudaMemcpy loop with real +=, like Chapter 8's reduce); all-to-all is a genuinely new pattern with no reduction and no ring shortcut, needing its own direct N*(N-1) cudaMemcpyPeer implementation (10.3). A single bundled host-side simulation verifies all three against independent references (reusing Chapter 8/9's own 10/20/30/40 and 100/104/108/112 numbers for continuity). All four code files genuinely compiled (nvcc for the three .cu, g++ for the .cpp) and run, deterministic. Cited to NCCL's own Collective Operations documentation (AllGather/ReduceScatter definitions) and Thakur/Rabenseifner/Gropp's MPICH collective communication paper (ring vs. recursive-doubling nearest-neighbor advantage, reduce-scatter+allgather=allreduce decomposition, Bruck's algorithm for short-message all-to-all). Written with ASCII diagrams throughout per the standing convention. This completes the standard six-collective set (broadcast, reduce, all-reduce, all-gather, reduce-scatter, all-to-all) this book builds by hand. Chapter 11 ("NCCL: What It Actually Does Differently From What You Just Built") written 2026-09-18: docs/part2/11-nccl-what-it-actually-does-differently.md, with code in docs/part2/code/ (30_nccl_comm_init.cu, 31_nccl_collectives_mapped.cu, 32_ring_vs_tree_model.cpp). **This completes Part 2.** First chapter to genuinely link against a real, installed NCCL library (apt libnccl2/libnccl-dev, version 2.18.3) rather than a hand-built substitute. Covers a real ncclCommInitAll() communicator, showing two genuinely different honest failures (ncclInvalidArgument for ndev=0, ncclUnhandledCudaError for ndev=1) (11.1); five of the book's six hand-built collectives (broadcast, reduce, all-reduce, all-gather, reduce-scatter) each matched to one real NCCL call, with all-to-all shown to have no dedicated call even in real NCCL -- built from ncclSend()/ncclRecv() grouped with ncclGroupStart()/ncclGroupEnd(), exactly like Chapter 10's own by-hand construction (11.2); and a closed-form ROUND-COUNT model (not fabricated timings) contrasting Chapter 9's own ring formula 2*(N-1) against a double binary tree's 2*ceil(log2(N)), citing NVIDIA's real NCCL 2.4 announcement (ring's linear latency scaling limitation, the tree's logarithmic fix, automatic ring/tree selection, and the real 24,576-GPU Summit test) (11.3). All three code files genuinely compiled (nvcc -lnccl for the two .cu against the real installed libnccl, g++ for the .cpp model) and run, re-verified from the committed repo copy before commit, deterministic. Written with ASCII diagrams throughout. Chapter 12 ("Data Parallelism: Replicated Model, Sharded Data") written 2026-09-18: docs/part3/12-data-parallelism-replicated-model-sharded-data.md, with code in docs/part3/code/ (33_replica_setup.cu, 34_gradient_allreduce.cu, 35_data_parallel_consistency_simulation.cpp). **Opens Part 3.** First chapter to put Part 2's collective-communication machinery to an actual use rather than building another primitive. Covers pure host-side data-shard partitioning (needs no device, genuinely succeeds) contrasted with a real ncclBroadcast() call distributing identical initial weights to every replica (12.1); a new real NCCL reduction op, ncclAvg, introduced alongside the ncclSum used since Chapter 8, called via ncclAllReduce() for gradient averaging (12.2); and a host-side simulation -- this chapter's real verification, since no real collective call could run -- proving that averaging gradients before every update keeps N replicas bit-identical to each other AND to an independent single-model reference trained on the mean target (PASS), while skipping synchronization causes silent, uncrashing divergence starting at step 1 (PASS, divergence correctly demonstrated). All three code files genuinely compiled (nvcc -lnccl for the two .cu against the real installed libnccl in the cloud sandbox, g++ for the .cpp) and run, re-verified from the committed repo copy before commit, deterministic. Cited to PyTorch's own DistributedDataParallel paper (Li et al., VLDB 2020) and DDP tutorial docs, and the Horovod paper (Sergeev & Del Balso 2018) for hvd.BroadcastGlobalVariablesHook -- both real, verified quotes tying data parallelism directly back to this book's own broadcast (Ch8) and all-reduce (Ch9/11). Written with ASCII diagrams throughout. Chapter 13 ("Model Parallelism: When One GPU Can't Hold the Weights") written 2026-09-18: docs/part3/13-model-parallelism-when-one-gpu-cant-hold-the-weights.md, with code in docs/part3/code/ (36_model_shard_memory_model.cpp, 37_layer_partition_activation_handoff.cu, 38_model_parallel_forward_simulation.cpp). Covers a closed-form memory-shard model reusing Chapter 1's own already-locked ZeRO 16-bytes-per-parameter figures as a self-consistency check, showing data parallelism (Chapter 12) multiplies total cluster memory but never reduces any single replica's own requirement (13.1); host-arithmetic layer-range partitioning structurally identical to Chapter 12's batch sharding, paired with a real cudaMemcpyPeer() call (Chapter 5's own API) moving a real, GPT-3-sized (Brown et al. Table 2.1: 96 layers, d_model=12288, nctx=2048) 50.33 MB activation tensor across a device boundary, honestly cudaErrorNoDevice (13.2); and a host-side correctness simulation proving a split forward pass is bit-identical (exact ==, not epsilon) to an unsplit one, since partitioning a strictly sequential computation only relocates where each step runs, never the order -- a strictly stronger guarantee than this book's own collectives could offer about floating-point summation order, contrasted explicitly with Chapter 8's reduce (13.3). Cites GPipe's real "severe under-utilization due to the sequential dependency of the network" quote to name (not yet quantify) the naive idle-time cost, deferred explicitly to Chapter 15; also cites Megatron-LM's "layer-wise pipeline parallelism" vs. "distributed tensor computation" distinction to preview Chapter 14. All three code files genuinely compiled (nvcc for the one .cu against the real installed toolchain in the cloud sandbox, g++ for the two .cpp files) and run, re-verified from the committed repo copy before commit, deterministic. Written with ASCII diagrams throughout. Chapter 14 ("Tensor Parallelism: Splitting a Single Matrix Multiply Across Devices") written 2026-09-18: docs/part3/14-tensor-parallelism-splitting-a-single-matrix-multiply-across-devices.md, with code in docs/part3/code/ (39_column_parallel_linear.cu, 40_row_parallel_allreduce.cu, 41_tensor_parallel_mlp_simulation.cpp). Megatron-LM's real "distributed tensor computation": a column-parallel GEMM splitting a weight matrix's OUTPUT columns across devices, each rank computing independently with NO real collective needed at all -- the first section in the whole book with no device-touching call beyond cudaGetDeviceCount(), since Megatron-LM's own paper states an elementwise nonlinearity can be "independently applied to the output of each partitioned GEMM" (14.1); a matching row-parallel GEMM splitting a second weight matrix's ROWS, producing genuine partial sums that require a real ncclAllReduce(ncclSum) to combine -- Megatron-LM's own quoted "the output of the second GEMM is then reduced across the GPUs" (14.2); and an end-to-end simulation chaining both into the real 2-GEMM MLP block, confirming Megatron-LM's own quoted claim of "only a single all-reduce operation in the forward pass" for the whole block via a direct collective-count comparison, while also being honest that -- unlike Chapter 13's guaranteed-exact sequential relocation -- a row-parallel combine is a real floating-point summation: empirically, 1 of 3 test inputs differed from the unsplit reference in the last representable bit, an explicit callback to Chapter 8's reduce non-associativity caveat (14.3). Notable toolchain finding: reproducing 14.3's exact bit-level claim required compiling with `-ffp-contract=off`, since this book's two real toolchains (cloud sandbox GCC 13.3.0, device GCC 11.4.0) defaulted to different FMA-contraction behavior at -O2 and initially disagreed on which input landed bit-identical -- resolved and cross-verified identical on both before locking the output. Also required requesting delete permission (device_request_delete_permission) again this session before `mkdocs build --strict` could clean its own site/ directory. All three code files (two .cu -- real nvcc/libnccl in cloud sandbox; one .cpp -- g++ on both cloud sandbox and device, cross-verified identical) genuinely compiled/run, re-verified from committed device-repo copies, output locked. Written with ASCII diagrams throughout. Chapter 15 ("Pipeline Parallelism: Splitting Layers Across Devices, and the Bubble That Costs You") written 2026-09-18: docs/part3/15-pipeline-parallelism-splitting-layers-across-devices-and-the-bubble-that-costs-you.md, with code in docs/part3/code/ (42_pipeline_schedule.cpp, 43_pipeline_stage_sync.cu, 44_bubble_and_pipelined_correctness.cpp). Quantifies and reduces the idle-time cost Ch13 named (GPipe's "severe under-utilization") but left unmeasured, WITHOUT changing Ch13's own model split. 15.1 builds the actual GPipe micro-batching pipeline schedule (microbatch m reaches device d at time t=m+d) for K=4,M=6, and counts busy/idle slots DIRECTLY rather than trusting a formula -- self-checks busySlots==K*M and idleSlots==K*(K-1) both PASS. 15.2 enforces one (device,microbatch) boundary from that schedule using Chapter 6's own real cudaEventRecord()/cudaStreamWaitEvent() mechanism, reused unchanged -- honestly cudaErrorNoDevice throughout, and the section's real point is that pipeline sync needs no new primitive, just Ch6's existing one called at the schedule's own boundaries. 15.3 confirms 15.1's counted fraction (12/36) exactly matches GPipe's own closed-form (K-1)/(M+K-1) formula, evaluates it at GPipe's own cited "negligible" threshold M=4xK for every real K this book has used (Ch13's own K=2 7B-model and K=35 GPT-3 splits, plus typical K=4,8 -- all land under 20%), and reuses Ch13's own toy 8-layer network + 3 test inputs verbatim as 3 micro-batches run through 15.1's interleaved schedule, proving every micro-batch's pipelined result is bit-identical to Ch13's own un-pipelined reference despite execution being interleaved across microbatches in time. Cited to GPipe's paper (already cited in Ch13; new quotes: the bubble formula itself and the M>=4xK negligible-overhead claim) and to Boehm's pipeline-parallelism explainer (clean algebraic rendering of the same formula, 1-m/(m+n-1), used to confirm GPipe's own less-legibly-typeset version). All three code files (two .cpp -- g++ with -ffp-contract=off on both cloud sandbox and device, cross-verified identical per Ch14's own established toolchain-safety practice; one .cu -- real nvcc in cloud sandbox) genuinely compiled/run, re-verified from committed device-repo copies, output locked. `mkdocs build --strict` passed on the device (delete permission from Ch14's session request still in effect). Written with ASCII diagrams throughout. **This completes the neural-network-parallelism arc of Part 3 (Chapters 12-15: data, model, tensor, pipeline parallelism).** Chapter 16 ("Domain Decomposition for Scientific Computing: Halo Exchange and Distributed Stencils") written 2026-09-18: docs/part3/16-domain-decomposition-for-scientific-computing-halo-exchange-and-distributed-stencils.md, with code in docs/part3/code/ (45_domain_decomposition_row_range.cu, 46_halo_exchange.cu, 47_stencil_decomposition_simulation.cpp). **Completes Part 3.** Leaves neural-network parallelism behind for the first time since Chapter 12: splits a literal physical grid (a discretized simulation domain), not a batch, model, matrix, or pipeline. 16.1 partitions grid rows into contiguous row-strips using the same host-arithmetic pattern as Chapter 12's computeShard()/Chapter 13's computeLayerRange() (computeRowRange()), introduces the real five-point stencil and Jacobi iteration formulas (cited to a Durham HPC teaching exercise), and establishes that -- unlike a data-parallel batch shard -- a grid row-strip needs real neighbor data every single iteration, not just at periodic synchronization points. 16.2 builds halo exchange, a genuinely new communication pattern for this book: strictly nearest-neighbor (each rank talks to at most 2 others), no privileged root (unlike Ch8), no ring wraparound (unlike Ch9 -- explicitly contrasted, since wrapping the domain's two physical edges together would be physically wrong), built from the same real cudaMemcpyPeer() Chapter 5 introduced and Chapter 13 reused, honestly cudaErrorNoDevice, with real boundary-vs-neighbor guards for the two edge ranks. 16.3 Part A is a host-side correctness simulation (4 local buffers standing in for 4 devices, halo exchange as array copy every iteration, real Jacobi double-buffering) proving a domain-decomposed 30-iteration Jacobi stencil is bit-identical (exact ==) to an unsplit reference, reusing Chapter 13's own structural relocation-not-reorder argument -- genuinely found and fixed a real 2-of-180-cell corner-boundary-ordering bug during development (row-vs-column boundary write order at the four corners), documented honestly in the code and Worked Solutions rather than silently fixed; Part B is a closed-form communication-volume model (not timed) showing halo-exchange traffic scales with a rank's boundary/perimeter while full-domain replication scales with its volume/area (10.7x less data at this chapter's own small grid, ~585x less at a realistic 1024x1024/8-way grid), cited to William Gropp's own halo-exchange lecture's "surface/volume behavior" quote. All three code files (two .cu -- real nvcc in cloud sandbox; one .cpp -- g++ with -ffp-contract=off on both cloud sandbox and device per Ch14's established practice, cross-verified identical) genuinely compiled/run, re-verified from committed device-repo copies, output locked. `mkdocs build --strict` passed on the device. Written with ASCII diagrams throughout, including a Chapter 9 ring-vs-halo-exchange contrast diagram and a Jacobi-vs-Gauss-Seidel double-buffering diagram. **This completes Part 3 (Chapters 12-16: data, model, tensor, pipeline parallelism, and domain decomposition).** Chapter 17 ("Barriers and Global Synchronization Across Devices") written 2026-09-18: docs/part4/17-barriers-and-global-synchronization-across-devices.md, with code in docs/part4/code/ (48_per_device_sync_is_not_a_barrier.cu, 49_nccl_allreduce_as_barrier.cu, 50_barrier_consistency_simulation.cpp). **Opens Part 4.** A cross-cutting concern applying to every Part 3 parallelization strategy rather than a new one of its own. 17.1 shows directly that looping Chapter 3's cudaSetDevice()/cudaDeviceSynchronize() pattern over every device only checks each device's own queue sequentially -- it never makes any device wait for any OTHER device's progress, honestly cudaErrorNoDevice throughout. 17.2 builds a real barrier the way distributed training code actually does, since NCCL's own Collective Operations docs list AllReduce/Broadcast/Reduce/AllGather/ReduceScatter/AllToAll/Gather/Scatter but no Barrier (the same kind of gap Ch11 found for all-to-all): a throwaway 1-element ncclAllReduce(), exploiting that it can't complete on any rank until every rank calls it, followed by a REQUIRED real cudaStreamSynchronize() since the NCCL call alone only enqueues asynchronously and does not itself block the CPU -- both calls genuinely compiled/linked against real nvcc+libnccl, three distinct honest codes (ncclUnhandledCudaError=1, ncclInvalidArgument=4, cudaErrorNoDevice=100). Contrasted with MPI's real dedicated MPI_Barrier() (Open MPI docs: "synchronization between MPI processes in a group... completes after all group members have entered the barrier"), previewing Part 5. Cites a real NVIDIA/nccl GitHub issue (#808, "Understanding Barriers") where a user independently observed a barrier "show up as a ringreduce kernel" -- external confirmation of the exact technique built here. 17.3 Part A is a host-side simulation with 4 ranks at heterogeneous simulated arrival times proving ONLY a barrier guarantees every rank reads the same complete shared checkpoint total (without one, only the last-arriving rank accidentally sees the correct value, 1 of 4 correct; with one, 4 of 4, PASS) -- explicitly generalizes Ch16's halo-exchange consistency argument beyond grid neighbors to any shared checkpoint; Part B is a closed-form cost model reusing Ch9's ring formula 2*(N-1) and Ch11's tree formula 2*ceil(log2(N)) UNCHANGED (not fabricated) to show a barrier's round count depends only on N, never on its (tiny, 1-element) payload size, contrasted with Ch5's own payload-size-dependent cost model. All three code files (two `.cu` -- real nvcc/libnccl in cloud sandbox; one `.cpp` -- g++, no floating point so no `-ffp-contract=off` needed, cross-verified identical on both cloud sandbox and device) genuinely compiled/run, re-verified from committed device-repo copies, output locked. `mkdocs build --strict` passed on the device. Written with ASCII diagrams throughout, including a per-device-sync-vs-global-barrier chapter-opening diagram, an NCCL-collectives-vs-MPI_Barrier contrast diagram, and a with/without-barrier race diagram.
Chapter 18 ("Load Balancing Across Heterogeneous GPUs") written 2026-09-18: docs/part4/18-load-balancing-across-heterogeneous-gpus.md, with code in docs/part4/code/ (51_heterogeneous_capability_query.cu, 52_weighted_shard_partition.cpp, 53_makespan_simulation.cpp). Picks up directly from Ch17's own heterogeneous-arrival-time simulation. 18.1 names the gap every Part 3 equal-split function (computeShard()/computeLayerRange()/computeRowRange()) has always had -- none ever asked how fast a given rank actually is -- via a real cudaGetDeviceProperties() query (honestly cudaErrorNoDevice), cited to a real, well-known heterogeneous-GPU-cluster paper (Um et al., "Cephalo," 2024: "training is bottlenecked by the slowest GPU, leaving faster GPUs idle," directly connected to Ch17's own barrier guarantee), plus real cited NVIDIA H100 whitepaper SM counts (H100 SXM5 132 SMs vs A100's 108, a real 22% gap) and an honest limit: multiProcessorCount can't see thermal throttling since the SMs are still physically present. 18.2 builds computeWeightedShard() -- the real Cephalo technique ("distributed unevenly across GPUs according to their relative computational speeds") -- splitting work proportional to measured speed instead of evenly; COMMON TRAP directly cites Cephalo's own documented pitfall that compute-proportional and memory-proportional splits are NOT the same thing ("GPU memory capacity doesn't always scale with compute speed"). 18.3 Part A: continuous-ideal algebra proving a proportional split makes every rank's finish time EXACTLY equal (totalWork/speedSum, the speed_i term cancels) regardless of speeds -- 2.00x makespan improvement for this chapter's own 4-device example; Part B: the SAME comparison using REAL integer-quantized shares from 18.2's own function, honestly showing the continuous ideal's exact equality does NOT survive rounding (1.333-unit spread, not 0.0) but the real speedup (1.99x) is still nearly identical to the ideal -- explicit callback to the Ch13-exact vs Ch14-epsilon distinction, this section's result belongs to the checked/epsilon kind. Caught and fixed a real missing-printf-argument bug in 52's own draft during development (silently printed 0 instead of 225) before locking output. All three code files (one `.cu` -- real nvcc in cloud sandbox; two `.cpp` -- g++, cross-verified identical on both cloud sandbox and device) genuinely compiled/run, output locked. `mkdocs build --strict` passed on the device. Written with ASCII diagrams throughout, including a chapter-opening equal-vs-weighted finish-time diagram and a continuous-ideal-vs-real-quantized contrast diagram.
Chapter 19 ("Stragglers, Failures, and Fault-Tolerant Collectives") written 2026-09-18: docs/part4/19-stragglers-failures-and-fault-tolerant-collectives.md, with code in docs/part4/code/ (54_blocking_vs_nonblocking_comm.cu, 55_nccl_async_detect_and_recover.cu, 56_backup_workers_simulation.cpp). **Completes Part 4.** Names the real, and worse, failure mode this book's own always-instant device-less errors (cudaErrorNoDevice/ncclUnhandledCudaError since Ch3/Ch11) could otherwise be mistaken for: a real cluster's genuinely dead peer causes every OTHER healthy rank to HANG, not error, at any collective or Ch17-style barrier waiting on it -- cited directly to NVIDIA's own "Building Scalable and Fault-Tolerant NCCL Applications" blog ("even healthy ranks should expect NCCL to either return an error or hang on any collective operation"). 19.1 builds both a default BLOCKING communicator (every earlier chapter's implicit config.blocking=1) and the real non-blocking alternative (ncclConfig_t, NCCL_CONFIG_INITIALIZER, config.blocking=0) side by side via ncclCommInitRankConfig(), both honestly failing identically in this device-less environment (code 1) -- the section's real point is naming the distinction that becomes visible only on a real cluster. 19.2 builds the real detect-and-recover sequence NVIDIA's docs describe: poll ncclCommGetAsyncError() in a bounded loop (their own real polling pattern, quoted verbatim) until it settles, then call the real ncclCommAbort() escape (quoted: "will exit any operation currently in progress, and destroy the communicator") -- explicit that abort is NOT recovery, just an escape from the hang, and a full rebuild (ncclCommInitRankConfig() again, all ranks, from scratch) is still required afterward; contrasts this with MPI's real, richer ULFM extension (MPIX_Comm_revoke()/MPIX_Comm_shrink(), quoted from Open MPI's own docs), previewing Chapter 20. 19.3 pivots to a genuinely different, milder, far more common real failure mode -- a worker that's merely SLOW this step, not dead -- via Chen et al.'s real "backup workers" technique ("Revisiting Distributed Synchronous SGD," arXiv 1604.00981: launch N+b workers, proceed once the first N report, drop the rest; their own real configuration N=96,b=4 out of 100 machines), verified against 3 fixed, named scenarios (no true straggler; a required worker catastrophically straggles; slow backups, no downside) checking the one invariant the technique promises -- waiting for backups is never slower than waiting for a fixed required set alone -- PASS in all 3/3 cases. COMMON TRAP explicitly distinguishes backup workers (straggler mitigation, assumes next-step recovery) from Section 19.2's machinery (genuine fault tolerance, no such assumption), and Worked Solutions Q8 uses this distinction to show what happens when permanently-dead workers exceed the backup budget b. Caught and fixed a real fabricated-quote error during development: an early draft of 55's code comments misquoted NVIDIA's blog with an invented sentence ("the only way to get out of this situation is...") that a follow-up WebFetch verification against the live page confirmed does NOT exist there -- corrected to the two real, verified quotes before locking, and re-compiled to confirm the comment-only fix changed nothing about the genuine output. All three code files (two .cu -- real nvcc/libnccl in cloud sandbox; one .cpp -- g++, cross-verified identical on both cloud sandbox and device, no floating-point exact-bit comparison so no -ffp-contract=off needed) genuinely compiled/run, output locked. `mkdocs build --strict` passed on the device. Written with ASCII diagrams throughout (strictly -, |, +, >, ^ only, re-checked programmatically for stray "<"/"v" arrow characters before locking). **This completes Part 4 (Chapters 17-19: barriers, load balancing, and fault tolerance).**
Chapter 20 ("MPI From Scratch, Then CUDA-Aware MPI: Scaling Beyond One Node") written 2026-09-18: docs/part5/20-mpi-from-scratch-then-cuda-aware-mpi-scaling-beyond-one-node.md, with code in docs/part5/code/ (57_mpi_ring_send_recv.c, 58_mpi_cuda_aware_query_and_fallback.cpp, 59_mpi_errhandler_return.c). **Opens Part 5.** First chapter in this book with a genuinely real multi-rank program: MPI needs no GPU at all (a multi-PROCESS model, not multi-device), so `mpirun` genuinely launches real OS processes for the first time -- no honest device-less error, no host-side simulation. Real toolchain work this chapter required: installed real Open MPI via `apt-get install libopenmpi-dev openmpi-bin` (gave a genuine Open MPI 4.1.6) in the CLOUD sandbox (the device has neither Open MPI nor root access to install it, confirmed this session -- MPI files, like `.cu`/NCCL files, can only be verified in the cloud sandbox). 20.1 builds real `MPI_Init`/`MPI_Comm_rank`/`MPI_Comm_size` plus a real point-to-point ring (Ch9's shape, now genuinely inter-process); confirmed real print-order non-determinism across 3 repeated runs (values always identical, order never was) -- something no earlier chapter's own single-process simulation could ever show. COMMON TRAP genuinely reproduced the MPI standard's own documented "unsafe program" ring deadlock (a naive Send-before-Recv ring genuinely hangs at 1,000,000-int messages, confirmed via a bounded `timeout 12` kill, exit code 124) and genuinely fixed it with `MPI_Sendrecv()`, both from the SAME committed file via optional argv parameters -- real, reproducible, not just asserted. 20.2 queries real CUDA-aware-MPI support two independent ways (`MPIX_CUDA_AWARE_SUPPORT` compile-time macro, `MPIX_Query_cuda_support()` runtime call) -- both genuinely 0/false for this exact apt-installed Open MPI build (confirmed independently via `ompi_info | grep cuda`: `opal_built_with_cuda_support=false`) -- then performs Ch5's own staged host-transfer fallback for real, reused between MPI ranks instead of GPU peers; first file in the book combining real mpicxx + real libcudart in one binary. 20.3 closes the loop Ch19 opened: verified, the same real way Ch19 verified NCCL's API surface, that ULFM is absent from this installed Open MPI (`ompi_info`'s own "MPI extensions:" list -- affinity, cuda, pcollreq, no ftmpi; grepped mpi-ext.h for MPIX_Comm_revoke/shrink -- absent), cited real ULFM history (merged into OMPI master 2020, stable from 5.0 -- this environment's 4.1.6 predates that), and HONESTLY reported a real attempt-and-failure to obtain a newer build: `download.open-mpi.org` blocked by network egress policy (confirmed via the agent-proxy status endpoint), `git clone` of the GitHub mirror succeeds but building from a raw checkout needs a full Autotools bootstrap judged too large a detour to gamble on -- then built standard MPI's own real, weaker alternative, `MPI_ERRORS_RETURN` (genuinely catches a real MPI_ERR_RANK error without aborting, rc=6, contrasted with the default MPI_ERRORS_ARE_FATAL's real abort+exit-code-6, verified separately since it's destructively fatal by design), explicit throughout that it is NOT a substitute for ULFM's own defined recovery semantics (MPI standard's own real quoted words: "the state of MPI is undefined" after an error, even under MPI_ERRORS_RETURN). **Caught and fixed a real misquote during drafting**: an early draft of the MPI-1.1 "state of MPI is undefined" quote stitched two separate sentences together in the WRONG order using an ellipsis, misrepresenting the original -- caught during a self-review pass (same discipline as Ch19's fabricated-NVIDIA-quote catch) and corrected to preserve the real sentence order, presenting both clauses as separately-attributed quotes instead of one falsely-continuous one. All three code files (two `.c` -- real mpicc; one `.cpp` -- real mpicxx linked against real libcudart, all against the real installed Open MPI in the cloud sandbox; NONE cross-verified on the device, which has neither Open MPI nor root access to install it) genuinely compiled/run, output locked. `mkdocs build --strict` passed. New `docs/part5/` directory and nav section created (first chapter of Part 5). Written with ASCII diagrams throughout, re-checked programmatically for stray "<"/"v" arrow characters before locking.
Chapter 21 ("GPUDirect RDMA: Bypassing the Host Entirely") written 2026-09-18: docs/part5/21-gpudirect-rdma-bypassing-the-host-entirely.md, with code in docs/part5/code/ (60_hybrid_mpi_nccl_bootstrap.cpp, 61_gpudirect_rdma_query.cu, 62_staged_vs_rdma_cost_model.cpp). 21.1 builds the real hybrid bootstrap every actual multi-node NCCL job uses but this book had never built: MPI (Ch20's own real multi-process model) generates and distributes NCCL's ncclUniqueId (rank 0 alone calls ncclGetUniqueId(), MPI_Bcast() delivers it to every other rank), then every rank independently calls ncclCommInitRank() with that SAME id and its own MPI-discovered rank/size -- verified against this exact environment's own installed nccl.h comment ("ncclGetUniqueId should be called once and the Id should be distributed to all ranks... before calling ncclCommInitRank") and NCCL's own official documentation's real MPI example ("in the context of MPI, using one device per MPI rank"); first file in the book combining real Open MPI + real NCCL + real CUDA runtime in one binary via mpicxx, genuinely run with mpirun --allow-run-as-root --oversubscribe -np 4, honestly ncclUnhandledCudaError (code 1) on every rank since no device exists here, with 3 genuinely different real print orderings captured across repeated runs exactly like Ch20's own Section 20.1. COMMON TRAP: removing the "only rank 0" guard around ncclGetUniqueId() would give every rank a different, independently-random id, so ncclCommInitRank() would have nothing to rendezvous on and would hang on a real cluster -- Ch19's own real, worse failure mode, now caused by a bootstrapping bug instead of a dead peer. 21.2 queries whether a GPU genuinely supports GPUDirect RDMA using three real CUDA device attributes confirmed directly against this exact environment's own installed driver_types.h rather than assumed from docs (cudaDevAttrGPUDirectRDMASupported, cudaDevAttrGPUDirectRDMAFlushWritesOptions, cudaDevAttrGPUDirectRDMAWritesOrdering), plus the matching cudaDeviceProp struct fields (Ch18's own query function, new fields) and a real cudaDeviceFlushGPUDirectRDMAWrites() call with real enum values -- all honestly cudaErrorNoDevice, the same failure family as every device-touching call since Ch3, not a new one. Cites GPUDirect RDMA's own design guide quote on the real mechanism ("PCI Express device issues reads and writes to a peer device's BAR addresses in the same way that they are issued to system memory"). COMMON TRAP distinguishes the device-level capability query (this section) from NCCL's own topology-dependent NCCL_NET_GDR_LEVEL (21.3) -- capable does not mean fast. 21.3 quantifies, using real numbers cited to NVIDIA's own "Benchmarking GPUDirect RDMA on Modern Server Platforms" post rather than a hardware measurement this environment cannot take, what bypassing the host is worth: Part A computes a 9.44x small-message latency speedup from the post's own two cited fixed-latency numbers (1.7-1.9us RDMA vs ~17us staged), matching the post's own "approximately 9x" claim; Part B reuses NCCL_NET_GDR_LEVEL's real PIX/PXB/PHB/SYS topology levels (Ch2's own topology-matters framing, now for a GPU-NIC pair) to show a real, cited 46.4x bandwidth swing (11.6 GB/s same-PCIe-switch vs 250 MB/s crossing inter-socket QPI) between two identically-"capable" systems; Part C honestly reports the post's own real 400-500KB crossover finding (staging becomes competitive beyond that size on Ivy Bridge Xeon) as a citation rather than re-deriving it, explicitly explaining why a simple model built from Parts A/B's own numbers should NOT be expected to reproduce an unpublished internal model -- consistent with this book's own practice of never quietly modeling around a gap in what a cited source actually reveals. All three code files (one .cpp -- real mpicxx linked against real libnccl + real libcudart; one .cu -- real nvcc against the real installed CUDA runtime; one .cpp -- g++, cross-verified byte-identical on both cloud sandbox and device) genuinely compiled/run in the cloud sandbox (Part 5's own standing toolchain constraint from Ch20: the device has neither Open MPI nor root access to install it), re-verified from committed device-repo copies, output locked. mkdocs build --strict passed. Written with ASCII diagrams throughout, re-checked programmatically for stray "<"/"v" arrow characters before locking -- two diagrams initially violated the character rule and were redrawn as horizontal boxes-and-arrows, same fix pattern as Ch19's own caught compliance slip.

PLANNING NOTE added 2026-09-18 (no chapters written yet, TOC only): the user asked for cutting-edge, real, well-documented industrial case studies to expand the book beyond Part 6's original 8, specifically requesting 2-3 from the financial services domain, in a separate new part rather than folded into Part 6. Added "Part 7 -- Cutting-Edge Industrial Case Studies" (Chapters 32-38), each selected because a real, citable, currently-active source exists and each ties back to a concept this book has already built: Ch32 Mixture-of-Experts/DeepEP (DeepSeek-AI's real DeepEP library, genuinely built on NVSHMEM -- this book's own Chapter 22 -- for GPU-initiated expert-parallel all-to-all dispatch/combine; also ties to Ch10's all-to-all and Ch14's tensor parallelism); Ch33 disaggregated LLM inference (DistServe, arXiv 2401.09670, prefill/decode split now standard in vLLM/SGLang/TensorRT-LLM production stacks; extends the already-planned Ch26 LLM inference case study); Ch34 GPU recommendation systems (NVIDIA Merlin HugeCTR, arXiv 2210.08803, a hybrid data-parallel-dense/model-parallel-embedding shape not covered by Ch12-14 individually); Ch35 AI weather forecasting (NVIDIA Earth-2/FourCastNet, real operational use e.g. DTN on AWS; callback to Ch16's domain decomposition, with a neural operator replacing the classic stencil); Ch36 multi-GPU XVA/portfolio risk at bank scale (real GPU-accelerated XVA pricing, cited to Kinetica's real industry writeups and NVIDIA's own GPU Gems finance chapter; extends the existing Ch31 Monte Carlo risk chapter into a genuinely distributed, multi-node, NCCL-aggregated pipeline); Ch37 low-latency market data and GPUDirect RDMA in capital markets (real NVIDIA Rivermax/financial-services blog posts on single-digit-microsecond latency; direct extension of this book's own Chapter 21 GPUDirect RDMA into a named financial application); Ch38 real-time multi-GPU fraud detection at payment scale (real NVIDIA/American Express case study on GPU-accelerated fraud detection). All seven need the same research-first discipline as every other chapter (fresh WebFetch/WebSearch verification of every quote before locking, per Ch19/Ch20/Ch21's own caught-misquote lessons) -- these are real, deployed, or actively-developed systems, not this book's own simulations, so citations matter even more here. Chapter order stays sequential (Part 5 finishes -- Ch20-23 -- then Part 6 -- Ch24-31 -- then this new Part 7 -- Ch32-38 -- before the Appendices);  do not jump ahead to Part 7 chapters until Parts 5 and 6 are actually done.

PLANNING NOTE added 2026-09-18 (no chapters written yet, TOC only): the user asked for 2 healthcare/medicine-related case studies to be added to Part 7 as well. Added Ch39 and Ch40, each grounded in a real, named, actively-maintained system with real vendor/peer-reviewed benchmark numbers: Ch39 genomics (real NVIDIA Clara Parabricks -- "over 100x faster analysis of whole-genome sequencing (WGS) compared to CPU-only solutions," "germline analysis from ~16 hours to under 10 minutes on 4 NVIDIA RTX PRO 6000 Server Edition GPUs," per its own docs at docs.nvidia.com/clara/parabricks; also has explicit multi-GPU scaling numbers, e.g. FQ2BAM 6.93 min on 2 GPUs -> 4.68 min on 4 GPUs -- ties to this book's own Ch10 all-to-all/Ch16 domain decomposition for partitioning genomic regions across devices); Ch40 medical imaging reconstruction (the real, peer-reviewed distributed ASTRA toolbox, Palenstijn et al., "A distributed ASTRA toolbox," Advanced Structural and Chemical Imaging, 2017 -- cone-beam backprojection "scaled nearly linearly to 21 GPUs" while forward projection showed a genuinely honest scaling LIMIT past 17 GPUs ("we hardly see any improvement in the execution time as it is dominated by the communication time"), reconstructing a 1984x1984x1332-voxel volume with 150 SIRT iterations on 20 GPUs -- ties directly to this book's own Ch16 domain decomposition/halo-exchange technique, now applied to volumetric medical reconstruction instead of a stencil, and gives this book a second genuinely honest communication-bound scaling-limit finding to sit alongside its own real toolchain failures). Considered NVIDIA BioNeMo (drug discovery/protein structure prediction, real 2.90x residue-normalized throughput improvement over the public Boltz-2 implementation) as a backup/alternate but chose genomics + imaging as the two strongest, most classically "healthcare" candidates with the clearest multi-GPU CUDA-programming angle (explicit GPU-count scaling data, not just single-node throughput). Same research-first, fresh-verification discipline applies when these are actually written. Part 7 is now Ch32-40 (9 chapters); sequential order unchanged -- finish Part 6 (Ch24-31) first.

Chapter 22 ("NVSHMEM and GPU-Initiated Communication") is DONE. Built and verified with the same discipline as every earlier Part 5 chapter: `nvidia-nvshmem-cu12` (pip, version 3.7.2) is a genuinely complete NVSHMEM distribution -- unlike the earlier-discovered incomplete `nvidia-cuda-nvcc-cu12` package -- shipping real headers, a real host shared library, real device bitcode for sm_70/80/89/90, and real bootstrap/transport plugins. Real API names and the `nvshmemx_status` error enum were verified by grepping the installed headers directly (`nvshmem.h`, `nvshmemx.h`, `non_abi/nvshmemx_error.h`), continuing Ch21's practice of checking installed headers rather than trusting web summaries when WebFetch of docs.nvidia.com returned empty content. Three files build an honest, escalating story: File 63 (`nvshmem_init()` with no bootstrap) genuinely falls back to a correct single-PE world (`n_pes=1`) -- not an error, since no discovery was ever requested. File 64 reuses Chapter 20's own real MPI infrastructure (`NVSHMEMX_INIT_WITH_MPI_COMM`) to give NVSHMEM a real 2-PE world, mirroring Chapter 21's own MPI-bootstrapped NCCL pattern -- `rc=0` on both PEs, though `nvshmem_malloc()` still honestly returns `NULL` since there is no real device to host a symmetric heap. File 65 builds the one genuinely new capability this book has never had: a CUDA kernel that calls `nvshmem_int_p()` itself, from device code, launched via `nvshmemx_collective_launch()` -- and this is the harshest real failure this book has produced: `rc=7` (`NVSHMEMX_ERROR_INTERNAL`), a separate `cudaDeviceSynchronize()` failure, `nvshmem_finalize()` itself unable to tear down, and the whole `mpirun` job exiting **255** rather than every earlier chapter's clean exit 0. Real compile/link recipe required unversioned `.so` symlinks (linker) plus the real `.so.N` directory (runtime loader) both on `LD_LIBRARY_PATH`, and `mpicxx` as `-ccbin` to drive `nvcc` (same trick Ch20/21 established). All three files re-verified byte-for-byte against the committed device-repo copies before locking. All four ASCII diagrams (chapter-opening + one per section) checked programmatically for the `-,|,+,>,^`-only rule; the drafted versions used a stray "v" for downward flow and a literal "<-"/"<<<>>>" and were redrawn as horizontal boxes-and-arrows before committing, the same fix already applied to Ch19 and Ch21. Committed as part of this same change.

Chapter 23 ("CUDA Graphs Across Multiple GPUs and Multiple Nodes") is DONE. **Completes Part 5 (Ch20-23: MPI, GPUDirect RDMA, NVSHMEM, multi-device/multi-node CUDA graphs).** 23.1 captured real work on two devices' streams into ONE graph via a genuine cross-device event edge (cudaEventRecord/cudaStreamWaitEvent captured together), verified against the CUDA Programming Guide's own cross-stream-dependency-during-capture language and NVIDIA's "graphs may also span multiple GPUs" blog claim; every one of 19 real API calls in the sequence -- even purely host-side graph bookkeeping in 23.3 -- honestly returned identical cudaErrorNoDevice, since the CUDA Runtime's implicit per-thread init fails once and every later call then genuinely checks that same state. 23.2 captured a real NCCL collective inside a graph (real since NCCL 2.9, confirmed via NCCL's own user guide), reusing Chapter 21's hybrid MPI+NCCL bootstrap -- and surfaced a genuinely new, sharper failure this book had not produced: calling ncclAllReduce() on a communicator from a FAILED ncclCommInitRank() segfaults the whole process inside libnccl.so.2 itself (no error code at all), independently re-verified to reproduce identically with or without graph capture, so the locked file adds a real defensive check (verify commErr==ncclSuccess before touching the handle again) that every earlier chapter's collectives didn't strictly need. 23.3 built a real cudaGraphAddHostNode() and showed, via the CUDA Runtime's own "no CUDA function may be called from callback" / "must not make any CUDA API calls" restrictions, why a host node cannot smuggle cross-process CUDA work into a graph -- the real multi-node pattern is each rank launching its own local graph, coordinated via an NCCL communicator bootstrapped by MPI entirely outside any capture. All three files re-verified byte-for-byte and output-exact against the committed device-repo copies before locking. All four ASCII diagrams checked programmatically for the -,|,+,>,^-only rule; the draft had a stray "v" and literal "<<<>>>"/"<-" and was redrawn, same fix pattern as Ch19/21/22. Committed as part of this same change.

Chapter 24 ("Multi-GPU Dense Matrix Multiplication at Scale") is DONE. **Opens Part 6's 8-chapter case-study arc.** 24.1: a real closed-form capacity model (reusing Ch1's own H100 SXM HBM figure) showing a GEMM's A and C operands, not the weight matrix B most engineers check first, are what force a real GPT-3-scale production batch (512x2048) past one device -- 242.25 GiB needed, minimum 4 real devices, versus B alone's 5.06 GiB. 24.2: SUMMA (Van de Geijn & Watts) built as a real host-side simulation over a 2D process grid -- column-panels of A broadcast within device rows, row-panels of B broadcast within device columns, local accumulation -- verified bit-exact against a naive reference on a 3x3/6x6x6 case AND re-confirmed on a second, non-square 2x2/8x12x4 case to rule out coincidence. 24.3: real NVIDIA cuBLAS-Xt (`cublasXtCreate`/`DeviceSelect`/`SetBlockDim`/`Sgemm`, real installed headers grepped directly) -- discovered a genuinely new, sharper failure: `cublasXtCreate()` succeeds even with zero devices (pure host bookkeeping, unlike every CUDA Runtime call since Ch3), but a FAILED `cublasXtDeviceSelect()` followed later by `cublasXtDestroy()` corrupts memory, caught by glibc's own allocator ("double free or corruption (fasttop)" / "free(): double free detected in tcache 2" on two separate runs) -- independently re-tested with `SetBlockDim()` removed to confirm it's specifically the failed-Select-then-Destroy sequence, not block sizing. The locked file adds a `cudaGetDeviceCount()` guard before ever calling `DeviceSelect()`, avoiding the corruption entirely. Closed with cuBLAS-Xt's own real single-node scope ("in a single node," per NVIDIA's product page) and cuBLASMp as NVIDIA's current real multi-node successor (cuBLASMg never left Early Access). New "Part 6 -- Case Studies" nav section added. All three files re-verified byte-for-byte against committed device-repo copies (69/70 cross-checked on both device and cloud sandbox since they're plain g++; 71 needs cuBLAS/nvcc, cloud sandbox only). All diagrams checked programmatically, zero violations this time. Committed as part of this same change.

Chapter 25 ("Distributed Training of a Neural Network: Data Parallelism and Ring All-Reduce") is DONE. 25.1: the real nested STEPS-times-BUCKETS `ncclAllReduce()` call structure a production training loop actually uses, reusing Ch21's own hybrid MPI+NCCL bootstrap, citing PyTorch DDP's own paper on why gradient bucketing exists specifically to let communication overlap with the still-running backward pass (25MB default bucket size; 38.0%/35.2% NCCL overlap speedup for ResNet/BERT). 25.2: a genuine multi-step distributed SGD training loop -- Ch12's own `computeShard()` pattern sharding 8 samples across 4 replicas, Ch9's own real ring all-reduce combining gradients every one of 20 real steps -- checked against a single-process full-batch reference. Found and fixed a real bug in the first `ringAllReduceSum()` implementation (it re-read a fixed neighbor's ORIGINAL value every round instead of forwarding what was just received, producing wildly wrong non-identical results); after the fix, 3 of 4 replicas match the reference exactly but Replica 3 differs by one bit (`w=3.221214771` vs `3.221214533`) -- confirmed genuine and reproducible by re-running twice with identical results, and independently cross-verified byte-for-byte on the device. This is Ch8's own non-associativity caution showing up for real inside ring all-reduce itself, specifically because this section's fp32 multi-step loop has neither Ch9's integer-only shelter nor Ch12's mean-target-trick shelter. 25.3: a real closed-form communication-cost model, applying Ch9's own `2(N-1)/N*K` formula unchanged to ResNet-50's real 25,557,032 parameters (torchvision's own model registry), quantifying why ring all-reduce's near-constant per-rank cost (approaching `2K` as N grows) beats a naive gather-then-broadcast's linearly-growing busiest-rank cost (`2(N-1)*K`, 256x worse at N=256) -- kept clearly separate from the real cited Horovod (88% TCP / >90% RDMA scaling efficiency at N=128) and PyTorch DDP overlap numbers, with an explicit COMMON TRAP against treating the closed-form ratio as a throughput prediction. All three files (72 NCCL/MPI-linked cloud-sandbox-only; 73 and 74 plain host-only, cross-verified byte-for-byte on both device and cloud sandbox) re-verified against committed device-repo copies with identical output. All diagrams checked programmatically -- 3 real violations found (standalone "v" used as a down-arrow in three diagrams) and fixed before locking. One new nav entry added under the existing "Part 6 -- Case Studies" section. Committed as part of this same change.

Chapter 26 ("Multi-GPU LLM Inference: Tensor and Pipeline Parallelism in Practice") is DONE. 26.1: a real combined 2D device grid (PP_STAGES x TP_DEGREE) -- Chapter 14's own Megatron-style column-parallel/row-parallel TP split, run entirely WITHIN each of Chapter 15's own pipeline stages -- built as a real host-side simulation using long long integer matrices (Ch24's SUMMA-style routing-only check), verified bit-exact against a naive unsplit reference across 4 microbatches. Established the real correctness property: a TP all-reduce never crosses a PP stage boundary, and a PP handoff only ever carries an already-combined activation. Explicitly distinguished inference's own forward-only pipeline schedule (M+PP_STAGES-1 steps, 20% bubble in the worked example) from Ch15's training-specific two-phase bubble formula, which does not apply here. 26.2: applied the combined shape to the real Megatron-Turing NLG 530B config (Smith et al.: 530B params, 8-way TP within a node, 35-way PP across nodes, 8x35=280 GPUs, quote-verified) and Megatron-LM's own real quote on why TP stays node-bound (inter-server links slower than NVLink). Found, via Ch9's own ring formula, a genuinely counter-intuitive real result: TP=280 (hypothetical, no PP) would move only 1.14x more data per rank than TP=8 (Ch9/25's near-flat-volume property holding again), but would need 39.9x more ROUNDS (558 vs 14) -- and every one of those extra rounds would cross the slower inter-server link, which is the REAL reason TP stays node-bound, not communication volume. 26.3: cited vLLM's own real super-linear KV-cache finding (TP=1->TP=2: 13.9x more KV cache blocks, 3.9x more token throughput, vs. a naive 2x expectation), built this chapter's own separate backward-solved worked model (weights occupying ~92.2% of one GPU's memory at TP=1 would produce exactly this ratio under a simplified capacity model) explicitly labeled as illustrative, not a reconstruction of vLLM's real internal accounting, and honestly stated that no published TP+PP combined throughput benchmark was found in vLLM/TensorRT-LLM docs or academic papers (citing TensorRT-LLM's own qualitative TP-vs-PP guidance instead of fabricating a number). All three files cross-verified byte-for-byte against committed device-repo copies (all plain .cpp, no CUDA/NCCL/MPI linkage -- cross-checked on both device and cloud sandbox). All diagrams checked programmatically, zero violations on first check. One new nav entry added. Committed as part of this same change.

Chapter 27 ("N-Body Simulation at Scale") is DONE. 27.1: real correctness simulation (N=12 bodies, P=3 shards) proving a distributed all-pairs N-body force calculation needs an ALL-GATHER (Ch10's own real collective), not Ch16's halo exchange -- verified bit-exact against a naive centralized reference using long long integers and a simplified (non-physical) pairwise interaction (Ch24/26's own routing-check technique), citing GPU Gems 3's real quote ("The total force F_i on body i... is obtained by summing all interactions," Nyland/Harris/Prins). COMMON TRAP: domain decomposition (splitting data across shards) does NOT by itself determine communication pattern -- that's determined by the physics/update rule, not the partition scheme. 27.2: real closed-form model reusing Ch9/10's own ring all-gather formula, applied to Burtscher & Pingali's real cited "5,000,000 bodies" scale -- found the opposite of Ch16's own halo-exchange finding: N-body's per-GPU communication volume GROWS toward the full 152.59 MiB dataset size as more GPUs are added (76.29 MiB at P=2 -> 150.20 MiB at P=64), never shrinking, because every GPU still needs every body's data every step, unlike Ch16's fixed-boundary halo volume which stayed flat. 27.3: real, named Barnes-Hut algorithm (Barnes & Hut 1986 Nature paper, real tree-subdivision quote) -- built a closed-form O(N^2) vs O(N log N) operation-count crossover model (ratio grows without bound, e.g. ~1.96M x at N=50M) and cited Burtscher & Pingali's real GPU Barnes-Hut measured numbers (74x faster than serial, 75 Gflop/s, kernel 5 over 90x faster) -- explicitly separated from this chapter's own (uncited) reasoning that Barnes-Hut's tree structure would plausibly also reduce multi-GPU communication, since no published source measures that specific claim. All three files cross-verified byte-for-byte (plain .cpp, no CUDA/NCCL/MPI). One diagram violation (stray "<-") found and fixed before locking. One new nav entry added. Committed as part of this same change.

Chapter 28 ("Distributed Breadth-First Search") DONE. Built on the real Yoo, Chow, Henderson, McLendon, Hendrickson & Catalyurek SC05 paper's own 1D vertex partitioning scheme and its own real worst-case finding, plus Beamer, Asanovic & Patterson's real SC12 direction-optimizing BFS paper, plus a real cited Graph500/Fugaku result. File 81 (host-side level-synchronous distributed BFS simulation, 12 vertices/3 ranks, deliberately including edges that skip a rank entirely) proved the distributed BFS's per-vertex level assigned exactly matches a naive centralized BFS reference (all 12 vertices EXACT MATCH), while separately recording which of the 9 possible (sender rank -> owner rank) pairs were actually used (7 of 9) -- making concrete Yoo et al.'s own real point that which ranks talk depends on the graph's actual structure and current frontier, unlike Ch16's fixed (r-1,r+1) halo pairs or Ch27's mandatory all-P-1 all-gather. File 82 quantified the real worst case Yoo et al.'s own paper states plainly ("for 1D partitioning, all P processors are involved in the communication operation"): a comparison table showing Ch28's 1D-BFS worst-case partner count (P-1) hits the SAME ceiling as Ch27's mandatory all-gather, but only as a graph-dependent CEILING, not a per-step guarantee -- then computed Yoo et al.'s own real 2D-partitioning fix (O(sqrt(P)) participants), whose reduction factor over O(P) grows with P (1.5x at P=4, 64.0x at P=4096). File 83 built an illustrative (explicitly labeled, not measured) frontier-fraction-vs-level model of Beamer et al.'s real direction-optimizing rationale, then closed with Yoo et al.'s real 2D-partitioning definition and a real cited Graph500 June 2024 result (Fugaku, SCALE 42, 166,029 GTEPS). Found and fixed one real logic bug during development: file 83's first version computed "unvisited fraction" as 1 minus only the levels STRICTLY BEFORE the current one, which left the current level's own frontier inside the "unvisited" count and made bottom-up's cost always exceed top-down's -- contradicting the chapter's own narrative that bottom-up gets cheaper at large-frontier levels. Fixed by explicitly excluding the current level's own frontier too (undiscovered = 1 - cumulativeBefore - f), re-verified the table now shows bottom-up cheaper starting exactly at the peak frontier level (level 3 onward), matching Beamer et al.'s real stated rationale. All 3 files are plain .cpp (no CUDA/NCCL/MPI/cuBLAS), cross-verified byte-identical between the cloud sandbox and the device via md5sum, and re-compiled/re-run from the committed device-repo copies with output matching the locked record exactly. Zero ASCII-diagram violations found in the chapter's 4 hand-drawn diagrams (the only "<-" characters detected were inside 3 locked verbatim terminal-output blocks, which are exempt from the diagram-drawing character rule since they are real program output, not hand-drawn diagrams). Committed alongside one new mkdocs.yml nav entry.

Chapter 29 ("Distributed Jacobi Solver") DONE. Combined Ch16's own row-strip domain decomposition/halo exchange with Ch8/9/11's own collective-reduction toolkit, applied to a genuinely different setting: an ITERATIVE linear solver rather than a fixed-step stencil sweep. Real citations: Saad's "Iterative Methods for Sparse Linear Systems" (2nd ed., SIAM 2003) for the Jacobi method itself, NVIDIA's own real multi-gpu-programming-models repo (mpi/jacobi.cpp -- "This project implements the well known multi GPU Jacobi solver," real MPI_Sendrecv top/bottom halo code, real "-nccheck: How often to check for convergence (default 1)" flag, real "delayed norm execution" variant), PETSc's real user manual on global-reduction blocking cost at scale ("above 10,000 processes... very time consuming"), James Demmel's real UC Berkeley CS267 lecture notes (Jacobi's real m~((n+1)/pi)^2 convergence formula, real Jacobi/CG/SOR/Multigrid complexity table, real Gauss-Seidel-converges-twice-as-fast relation), and NVIDIA's real AmgX paper (Naumov et al., SIAM J. Sci. Comput. 2015 -- real cited "2-5x speedup on a single GPU against a competitive implementation on the CPU"). File 84: host-side simulation of P ranks (1,2,3,4,6,12) row-strip-decomposing a 12x8 Poisson grid, verified the Jacobi UPDATE step matches a single-process reference bit-for-bit at every P (Ch16's own finding holds unchanged) -- but found a REAL, genuine one-bit mismatch in the GLOBAL RESIDUAL NORM at P>1 (1.3874786747088721 vs ...088719) when computed as P separate per-rank partial sums combined afterward vs. a single flat pass, a direct real instance of Ch8/Ch25's own floating-point non-associativity caution showing up for a new reason (grouping, not reduction algorithm). File 85: closed-form round-count model contrasting the halo exchange's fixed P-independent cost against the residual Allreduce's P-dependent round cost (naive ring 2(P-1) vs tree 2*ceil(log2(P)), reusing Ch9/11's real models), and quantified NVIDIA's own real nccheck=K amortization (overhead reduces by exactly K). File 86: computed Demmel's real formula at real grid sizes (n=10 to 10,000, showing Jacobi needs ~2*10^8 iterations at n=10,000 for 1e-6 error) plus the real complexity table, closing with AmgX as the real production successor reusing this chapter's exact communication pattern around Multigrid instead of Jacobi. All 3 files plain .cpp, cross-verified byte-identical cloud sandbox vs. device via md5sum, re-compiled/re-run from committed device copies with output matching the locked record exactly. 3 diagram violations (standalone "v" used as a down-arrow in the chapter-opening diagram) found and fixed before locking -- replaced with plain vertical-bar connectors relying on top-to-bottom position to imply flow, matching this book's own established convention (no down-arrow glyph is in the allowed character set). Committed alongside one new mkdocs.yml nav entry.

Chapter 30 ("Multi-GPU Ray Tracing and Rendering") DONE. Broke the pattern every prior Part 6 case study followed (per-step communication) with the opposite extreme: rendering's clean case needs ZERO communication during computation. Real citations: Molnar, Cox, Ellsworth & Fuchs's real classic taxonomy ("A Sorting Classification of Parallel Rendering," IEEE Computer Graphics and Applications, 1994 -- real "sort-last" definition, deferring assembly "until the end of the rendering pipeline"), Wikipedia's real render-farm summary ("frames and sometimes tiles can be calculated independently... main communication... upload of source material... download of finished images"), NVIDIA's own real Omniverse RTX renderer docs (real "splits the rendering of the image into a large tile per GPU with a small overlap region between them" and real "automatically balances the amount of total path tracing work" quotes), and Fouladi et al.'s real SIGGRAPH 2022 R2E2 paper (real "scenes with up to a terabyte of geometry and texture data (where as little as 1/250th of the scene can fit on any one node)" and real "3.5-7.8x faster than pbrt-treelet's single-machine in-memory path tracer" cited speedup). File 87: a tiny deterministic 2-circle integer-only "scene" (no floating point, no shared state) rendered via row-tiles across P ranks (1,2,3,4,6,12) -- verified bit-exact match against a single-process reference at every P, with NO reduction step at all (unlike every earlier chapter's collective), so no associativity question even arises. File 88: closed-form model contrasting this chapter's fixed one-time final-image cost against every earlier chapter's per-step-growing cost, then modeled NVIDIA's own real automatic load-balancing using Chapter 18's real weighted-sharding technique applied to uneven per-row rendering cost -- found a real 1.50x reduction in the slowest rank's load via a non-contiguous row assignment, only possible because rendering tiles (unlike Ch16/29's halo-exchange strips) have no neighbor dependency. File 89: R2E2's real terabyte-scale citation showing ~250 nodes needed just to hold a real production scene, reintroducing genuine cross-node communication mid-render once the scene itself (not just the image) no longer fits per device -- rejoining the rest of this book's distributed-systems concerns. All 3 files plain .cpp, cross-verified byte-identical cloud sandbox vs. device via md5sum, re-compiled/re-run from committed device copies matching the locked record exactly. 2 diagram violations (a "<--...-->"-style arrow and a stray "<-" annotation) found and fixed before locking -- replaced with plain "---" connectors and parenthetical text, keeping to the allowed -,|,+,>,^ character set. Committed alongside one new mkdocs.yml nav entry.

Chapter 31 ("Multi-GPU Monte Carlo Risk Simulation") DONE. CLOSES Part 6 (Ch24-31, 8 case-study chapters complete). Showed Monte Carlo risk simulation sits between Ch30's zero-reduction rendering case and every earlier Part 6 chapter's per-step communication: independent paths like Ch30's pixels, but always ending in a real reduction Ch30 never needed. Real citations: Boyle's real classic 1977 Monte Carlo option-pricing paper (Journal of Financial Economics), NVIDIA's own real "Monte Carlo Option Pricing" whitepaper (Podlozhnyuk & Harris, 2008 -- real contiguous-subset multi-GPU split, real sum/sum-of-squares-per-thread, real parallel-reduction and discount-factor quotes), NVIDIA's real cuRAND documentation (real Philox counter-based family quote, real "each thread of computation should be assigned a unique id number" independence guidance), Chan/Golub/LeVeque's real 1979 Stanford pairwise variance-combination paper (real formula verified via Wikipedia's "Algorithms for calculating variance"), the Basel Committee's real 1996 market-risk amendment (real "99th percentile, one-tailed confidence interval" quote), and Jorion's real VaR textbook definition. File 90: 24 simulated paths (counter-based deterministic hash per path, no floating point in the payoff itself) split across P=1..24, combined via Chan et al.'s real pairwise formula -- found a REAL floating-point mismatch: combined path COUNT and integer sums match exactly at every P (integer addition is exactly associative), but combined M2 (variance) does NOT match at P>1, a new real instance of Ch8/25/29's non-associativity finding. File 91: cited cuRAND's real Philox independence property (why zero RNG coordination is possible in production, contrasted against what a naive stateful sequential RNG would require), then found an EVEN SHARPER real result: holding the SAME P=8 partition and SAME per-rank partial stats fixed, combining them via a sequential fold (7 rounds) vs. a tree fold (3 rounds) -- both using the identical real Chan et al. formula at every step -- produced a DIFFERENT M2, proving reduction TOPOLOGY alone (not just partitioning) changes a floating-point result, independent of Ch9/11's own round-count performance argument for trees. File 92: real Basel 99th-percentile VaR citation showed Chan et al.'s O(1)-per-rank summary genuinely cannot produce an exact percentile -- verified an exact VaR needs every rank's FULL local share of raw data gathered (reusing Ch10/27's all-gather pattern), contrasting O(1) message size (mean/variance) against O(N/P) message size (exact percentile); named real streaming-percentile sketches (t-digest-style) as production's actual answer to avoid full gathering at scale, without building that extension. All 3 files plain .cpp, cross-verified byte-identical cloud sandbox vs. device via md5sum, re-compiled/re-run from committed device copies matching the locked record exactly. Zero diagram violations on first check. Committed alongside one new mkdocs.yml nav entry.

Chapter 32 ("Mixture-of-Experts at Scale: Expert Parallelism and GPU-Initiated All-to-All (DeepEP)") is DONE (commit pending this session) -- opens "Part 7 -- Cutting-Edge Industrial Case Studies". 32.1 built a host-side MoE dispatch/combine correctness simulation (File 93) using real GShard paper terms (Lepikhin et al. 2020: top-2 gating, expert capacity, and the real residual-passthrough behavior for fully overflowed tokens) -- found a real overflow-induced NaN bug from an unstabilized softmax during drafting, fixed it, then confirmed MoE combine is BIT-EXACT at every P tested (1..24), a new and different reason than Chapter 30's rendering for avoiding the non-associativity Chapter 8/25/29/31 kept finding: MoE combine never sums a value ACROSS ranks for one token's own result. 32.2 (File 94) presented DeepSeek-AI's own real DeepEP library's two published kernel-family benchmark tables (docs/legacy.md) verbatim -- normal kernels (153-158 GB/s intranode, 43-58 GB/s internode) vs. low-latency kernels (77-194us dispatch latency, 39-98 GB/s, falling as EP grows) -- showing neither dominates the other, echoing Chapter 18's own load-balancing finding. 32.3 (File 95) compiled and ran REAL NVSHMEM 3.7.2 APIs (nvshmem_int_alltoall, nvshmemx_collective_launch calling nvshmem_int_p from a kernel -- the real Chapter 22 pattern) in the cloud sandbox, discovered a genuinely new toolchain fact (need -arch=sm_90 to avoid an ambiguous half-type conversion error in NVSHMEM's own headers under nvcc's default generic-arch mode), confirmed real MPI bootstrap success (my_pe/n_pes correct) and hit the same honest "zero real CUDA devices" limitation Chapter 22/23 already established, handled gracefully (exit=0). Also caught, via a fresh live read of deepseek-ai/DeepEP's actual GitHub repo, that DeepEP's own V2 release moved from NVSHMEM to a "NCCL Gin backend" -- documented honestly as a real architecture change, not assumed unchanged from memory. Zero ASCII diagram violations on first check (matches Chapter 31). Next: Part 7, Chapter 33 (disaggregated LLM inference / DistServe, extending the planned Ch26 case study).
Chapter 33 ("Disaggregated LLM Inference: Prefill/Decode Splitting (DistServe)") is DONE (commit pending this session) -- extends this book's own Chapter 26 case study. 33.1 (File 96) built an illustrative prefill-vs-decode cost model (Chapter 1's arithmetic-intensity framing) plus real DistServe paper (Zhong et al. 2024, arXiv 2401.09670) cited numbers -- goodput definition, real 4.48x/10.2x measured improvement. 33.2 (File 97) built a clean INTEGER-ONLY (deliberately not floating-point, to avoid entangling with the Ch8/25/29/31/32 associativity question) prefill+decode simulation confirming a request's prefill TP degree (Tp) and decode TP degree (Td) can differ freely -- all 16 (Tp,Td) combos in {1,2,4,8}x{1,2,3,6}, including non-recommended Tp>Td shapes, matched the single-process reference bit-exact -- confirming NVIDIA Dynamo's own real "smaller TP for prefill, larger TP for decode" design is correctness-preserving, not merely a performance choice. 33.3 (File 98) used DistServe's own real cited KV cache size (1.13GB, OPT-66B/512 tokens) and real cited NVLink (600GB/s)/InfiniBand (800Gbps) bandwidths to compute the real transfer time (~1.9ms NVLink, ~11.3ms InfiniBand) behind DistServe's own "negligible overhead" conclusion -- named as this book's first genuinely POINT-TO-POINT (not collective) communication shape since Ch5/Ch21, plus NVIDIA Dynamo's real NIXL-based non-blocking transfer design. Zero ASCII diagram violations on first check (3rd chapter in a row). Just ONE mkdocs.yml nav entry added under the existing Part 7 section (no new section needed). Next: Part 7, Chapter 34 (GPU recommendation systems / NVIDIA Merlin HugeCTR, combining Ch12-14's data/model/tensor parallelism within one hybrid model for the first time).
Chapter 34 ("GPU Recommendation Systems: Hybrid Data-Parallel and Model-Parallel Embeddings (HugeCTR)") is DONE (commit pending this session) -- real NVIDIA Merlin HugeCTR paper (Oldridge et al. 2022, arXiv 2210.08803). 34.1 (File 99) built a capacity model (Ch1 memory-wall + Ch13 memory-shard formula) using HugeCTR's own real cited ~640GB practical single-GPU limit, showing pure data parallelism fails past that scale while the tiny dense DNN comfortably replicates at any scale -- the real reason for HugeCTR's own HYBRID design (Ch12 DP for dense layers + Ch13 MP for embeddings, applied to two DIFFERENT PARTS of one model, a first for this book). 34.2 (File 100) built an INTEGER-only (again deliberately, isolating from the Ch8/25/29/31/32 float-associativity theme) resharding correctness simulation of HugeCTR's own real embedding all-to-all ("after each local reduction step, all-to-all communication is conducted to share the results of the lookup operation... along the batch dimension") -- bit-exact at every P, and explicitly contrasted against Ch32's MoE all-to-all: this one follows a FIXED schedule with no gating decision and no capacity-overflow equivalent, a genuinely different reason for exactness than Ch32's. 34.3 (File 101) reused Ch9's own real (P-1)/P round-count formula to show per-GPU memory shrinks LINEARLY without limit as P grows (Ch1) while the all-to-all's own off-rank communication FRACTION saturates near 100% by moderate P (87.5% at P=8, 99.2% at P=128) -- Ch16's "more ranks is not free" lesson, rediscovered here for a structurally different reason (a saturating fraction, not a fixed floor). Zero ASCII diagram violations on first check (4th chapter in a row). Next: Part 7, Chapter 35 (AI weather forecasting / NVIDIA Earth-2 FourCastNet, extending Ch16's domain decomposition to a learned neural operator).
Chapter 35 ("AI Weather Forecasting at Scale," NVIDIA Earth-2 / FourCastNet) is DONE (commit pending this session) -- corrects, in print, its own predecessor chapter's closing preview (which predicted FourCastNet would extend Ch16's halo exchange): real FourCastNet paper (Pathak et al. 2022, arXiv:2202.11214). 35.1 (File 102) found FourCastNet's real 720x1440 global grid comfortably fits one real A100, so its real cited 64-GPU/16-hour training run is ordinary Ch12 data parallelism, NOT Ch16 domain decomposition -- a genuine, honest correction of the halo-exchange assumption. 35.2 (File 103) showed WHY that assumption would eventually break anyway: AFNO's own real Fourier-domain mixing ("continuous global convolution, implemented efficiently in the Fourier domain with FFTs") is fundamentally GLOBAL, so Ch16's LOCAL halo exchange could never supply what it needs once a grid outgrew one GPU -- built and verified NVIDIA's own real cuFFTMp slab-decomposition-plus-transpose technique (Ch10's all-to-all primitive) as a host-side 2D DFT correctness simulation, bit-exact at every tested P (1,2,4,8), echoing Ch34's own resharding-not-reduction finding for a genuinely different underlying computation. 35.3 (File 104) separated correctness from cost using cuFFTMp's own real cited benchmark numbers (78ms@8 GPUs -> 4ms@2048 GPUs for a 2048^3 FFT, >70% peak bandwidth, 1.8 PFlop/s at 4096 GPUs) contrasted against FourCastNet's own real grid -- about 8285x smaller than that benchmark, the honest reason this chapter's own distributed-FFT technique is not needed today but would be at a future, much higher-resolution model. Zero ASCII diagram violations on first check (5th chapter in a row). Next: Part 7, Chapter 36 (multi-GPU XVA/portfolio risk, extending Ch31 Monte Carlo).
Chapter 36 ("Multi-GPU XVA and Portfolio Risk") is DONE (commit pending this session) -- extends Chapter 31's own Monte Carlo risk simulation with real citations from STAC-A2 (NVIDIA's real 8x Tesla V100 "8.9x the next best throughput" result and a real 8x A100 "Monte Carlo estimation of Heston-based Greeks... with early exercise" result), Oracle Financial Services Analytics documentation, and a real independent arXiv XVA paper (Albanese et al., arXiv:2009.00368). 36.1 (File 105) showed a real XVA exposure PROFILE is Chapter 31's own single reduction repeated T times along a NEW axis (real observation dates, T=360 from Oracle's own real cited "30Y:1M" tenor example), reusing Chapter 9's own real round-count formula completely unchanged -- a new SCALING axis, not a new technique. 36.2 (File 106) found this chapter's one genuinely new idea: a netting set's trades must be summed BEFORE a zero floor is applied (Oracle's own real "Positive Netted... sum of positive exposures calculated at the netting set level" quote), which follows from a real, provable convexity inequality (max(0,a+b) <= max(0,a)+max(0,b)) -- verified across 500 random portfolios with ZERO violations and a real 58.12% average exposure reduction from netting, a real economic (not floating-point) order-sensitivity, genuinely different in KIND from every earlier Ch8/25/29/31/32/35 associativity finding. 36.3 (File 107) combined both real extensions (Ch12 path-splitting + Section 36.2's local netting + Ch31's own Chan/Golub/LeVeque formula reused once per date) and found a SHARPER version of Ch31's own finding: because real exposure is continuous (unlike Ch31's deliberately-integer test payoff), even the EPE MEAN itself, not only variance, became P-dependent (tiny ~1e-15 mismatches at P>1, exactly zero only at P=1 and P=24 by coincidence of the test data). Zero ASCII diagram violations after one fix (a stray "<=" inside a text-block diagram, caught and replaced with prose) -- 6th chapter in a row clean after that one fix. Next: Part 7, Chapter 37 (GPUDirect RDMA in capital markets, extending Ch21).
Chapter 37 ("GPUDirect RDMA in Capital Markets") is DONE (commit pending this session) -- extends Chapter 21's own GPUDirect RDMA to low-latency trading, real citations: NVIDIA's own real Databento case study, a real NVIDIA Rivermax/NEIO FastSocket financial-services blog, a real NVIDIA capital-markets inference latency blog, and NVIDIA/gdrcopy's own real GitHub README. 37.1 (File 108) established the real, CURRENTLY-DEPLOYED baseline: kernel-bypass DPDK NIC processing gets market data to HOST memory at real production scale ("exceeds 80 Gbps," "over 200 billion market updates each day") and a real cited sub-60us median latency -- stopping short of GPU memory -- and explicitly flagged a real risk of dividing a peak-capacity figure by a daily-average-volume figure (produced a 4320-byte "average message size" and called it out as a mismatch, not a real number, rather than quietly reporting it). 37.2 (File 109) found and quoted NVIDIA's OWN real "What's next" framing: NIC-to-GPU GPUDirect RDMA for market data specifically is aspirational in NVIDIA's own real documentation, not yet a deployed result like Ch21's own GPU-to-GPU/storage use cases -- built an explicitly-labeled ILLUSTRATIVE projection (Ch21's real 9.44x speedup applied to File 108's real sub-60us baseline, -> ~6.36us) repeatedly flagged as a projection, never a finding, echoing Ch35's own honest-synthesis framing device. 37.3 (File 110) found a DIFFERENT real, currently-shipping use of GPUDirect RDMA running the OTHER direction -- NVIDIA's own real GDRCopy library lets the CPU read GPU memory directly (real cited ~1.09us A40 round trip) vs. an ordinary cudaMemcpy's real cited 6-7us overhead -- set against real cited single-digit-microsecond capital-markets P99 inference budgets (4.3-4.61us, GH200/RTX PRO 6000), found cudaMemcpy's own fixed overhead ALONE would consume 130%-140% of the ENTIRE real budget, a genuinely NEW "fixed overhead dominates" finding inverting every earlier Ch5/9/16/27/28/34/35 "volume eventually dominates" cost-model lesson. Caught and fixed a real missing-printf-argument bug in File 109 during drafting (matching Ch18's own earlier real finding of the same bug class) before locking. Zero ASCII diagram violations on first check (7th chapter in a row). Next: Part 7, Chapter 38 (real-time fraud detection at payment scale, real NVIDIA/American Express case study).
PART 6 COMPLETE (Chapters 24-31, 8 case-study chapters). PART 7 now underway (Chapters 32-37 DONE, opened "Part 7 -- Cutting-Edge Industrial Case Studies"). Next: Chapter 38 ("Real-Time Fraud Detection at Payment Scale," real NVIDIA/American Express case study). Continue the same build-verify-lock pipeline: research citations first (WebSearch/WebFetch, with a fresh-verification pass on any quote pulled from memory), verify exact real API names against this environment's own installed headers before writing any code, ASCII diagrams from the start (-,|,+,>,^ only, verify programmatically before locking -- remembering that locked verbatim terminal-output blocks are exempt from this character rule since they're real program output, not hand-drawn diagrams, and that arrows must never use "<" -- use "---" or restructure instead), all NCCL/MPI/cuBLAS/NVSHMEM-linked files compiled+run in the CLOUD sandbox only (never device_bash for those -- though plain .cpp files with no CUDA/NCCL/MPI/NVSHMEM can be cross-verified on the device too), add one nav entry under the existing "Part 7" nav section (no new section needed), commit with the standard attribution footer, give the user both git push and mkdocs build --strict together, verify current remote state rather than assuming, and re-request delete permission via device_request_delete_permission early each NEW session if mkdocs build --strict needs to clean site/ (grants do NOT carry over between sessions/containers). Part 7 is 9 chapters (Ch32-40, including 2 healthcare/medicine case studies -- Ch39 genomics/Parabricks, Ch40 medical imaging/distributed ASTRA) and follows before the Appendices.
